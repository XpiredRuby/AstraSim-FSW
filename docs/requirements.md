# Requirements document moved

The canonical ASTRA-OS requirements baseline is [`docs/REQUIREMENTS.md`](REQUIREMENTS.md).

This lowercase path is retained only for compatibility with existing links. Do not add or modify requirement rows here. The traceability checker reads the canonical uppercase file and [`docs/VERIFICATION_MATRIX.csv`](VERIFICATION_MATRIX.csv).
